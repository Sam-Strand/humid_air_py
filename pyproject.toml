[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "humid_air"
version = "1.0.3"
authors = [
    {name = "Садовский М.К.", email = "i@maxim-sadovskiy.ru"},
]
license = {text = 'Apache-2.0'}
readme = "README.md"
requires-python = ">=3.7"
classifiers = [
    "License :: OSI Approved :: Apache Software License",
]
dependencies = [
    "numpy>=1.20.0",
    "numba>=0.55.0"
]

[project.urls]
Repository = "https://github.com/Sam-Strand/humid_air_py"
